CREATE TABLE users (
    id_user INTEGER NOT NULL GENERATED BY DEFAULT AS IDENTITY (START WITH 1, INCREMENT BY 1),
    user_name VARCHAR(60) NOT NULL UNIQUE,
    password VARCHAR(60) NOT NULL,
    db_name VARCHAR(60) NOT NULL,
    PRIMARY KEY(id_user)
);
